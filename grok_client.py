"""Клиент для работы с Grok API (console.x.ai) и генерации постов для Threads."""
from __future__ import annotations

import os

import requests

XAI_API_URL = "https://api.x.ai/v1/chat/completions"

# Три тематики аккаунта. key -> (название, описание/стиль для промпта)
TOPICS: dict[str, dict[str, str]] = {
    "management": {
        "title": "Менеджмент",
        "persona": (
            "Ты — практикующий эксперт по менеджменту и управлению командами. "
            "Пишешь для блогерского экспертного аккаунта. Даёшь прикладные советы, "
            "разбираешь управленческие ситуации, лидерство, продуктивность, найм, "
            "делегирование и рост команд."
        ),
    },
    "psychology": {
        "title": "Психология",
        "persona": (
            "Ты — эксперт по практической психологии. Пишешь для блогерского "
            "экспертного аккаунта. Объясняешь поведение людей, эмоции, отношения, "
            "мотивацию и ментальное здоровье простым и поддерживающим языком, "
            "без псевдонаучных обещаний."
        ),
    },
    "philosophy": {
        "title": "Философия",
        "persona": (
            "Ты — эксперт по философии, который умеет объяснять сложные идеи просто. "
            "Пишешь для блогерского экспертного аккаунта. Рассуждаешь о смысле, этике, "
            "сознании, свободе, счастье и мышлении, опираясь на идеи философов, "
            "но говоришь живым современным языком."
        ),
    },
}

DEFAULT_MODEL = os.getenv("XAI_MODEL", "grok-4.5")

# Роли-амплуа блогера. key -> (название, короткое пояснение для UI, развёрнутая
# инструкция по стилю/формату для промпта Grok).
ROLES: dict[str, dict[str, str]] = {
    "storyteller": {
        "title": "Сторителлер (Кинематографист)",
        "tagline": "Сюжетные сериалы из будней",
        "persona": (
            "Пиши в амплуа «Сторителлер (Кинематографист)». Ты превращаешь обычные "
            "рабочие будни в маленькое кино: у поста есть завязка, поворот и развязка. "
            "Начинай со сцены или конкретного момента («Вторник, 9 утра, и всё пошло "
            "не так…»), веди читателя за руку через события, показывай детали, эмоции "
            "и внутренний монолог героя, а не сухие выводы. Используй настоящее время "
            "для эффекта присутствия, короткие абзацы как кадры, ощущение продолжения "
            "истории (как серия сериала). В финале — ненавязчивый смысл или крючок, "
            "который хочется досмотреть в следующем посте. Показывай, а не рассказывай."
        ),
    },
    "philosopher": {
        "title": "Философ (Вдохновитель)",
        "tagline": "Разговоры о ценностях и смыслах",
        "persona": (
            "Пиши в амплуа «Философ (Вдохновитель)». Ты размышляешь вслух о ценностях, "
            "смыслах и внутренних опорах человека. Задавай большие, но честные вопросы, "
            "не давя моралью и не скатываясь в банальности из пабликов. Опирайся на "
            "наблюдение из жизни, доводи его до глубокой мысли, оставляй читателю "
            "пространство подумать самому. Тон тёплый, неторопливый, вдохновляющий, "
            "но без пафоса и эзотерики. Хорошо работают контраст «как принято — как на "
            "самом деле» и мягкий призыв посмотреть на привычное под новым углом."
        ),
    },
    "backstage": {
        "title": "Жизнь внутри (Кухня)",
        "tagline": "Живое закулисье офиса/цеха",
        "persona": (
            "Пиши в амплуа «Жизнь внутри (Кухня)». Ты показываешь честное закулисье: "
            "как всё устроено на самом деле за кадром — процессы, провалы, смешные и "
            "неловкие моменты, внутренние ритуалы команды. Дай эффект «пустили за "
            "кулисы»: конкретные детали, живые диалоги, немного самоиронии, ноль "
            "глянца и рекламы. Читатель должен почувствовать доверие и причастность, "
            "будто ему по секрету рассказали, как это выглядит изнутри."
        ),
    },
    "opinion_leader": {
        "title": "Лидер мнений",
        "tagline": "Аналитика и прогнозы по рынку",
        "persona": (
            "Пиши в амплуа «Лидер мнений». Ты уверенно разбираешь, что происходит в "
            "индустрии, и даёшь свой обоснованный прогноз. Начинай с наблюдения или "
            "тренда, объясняй причину и следствие, формулируй чёткую авторскую позицию "
            "(«я считаю, что…»). Оперируй логикой и примерами, а не общими словами; "
            "будь конкретным и немного смелым в выводах, чтобы за мнением хотелось "
            "следить. Тон экспертный, собранный, без воды."
        ),
    },
    "interviewer": {
        "title": "Интервьюер (Шаман)",
        "tagline": "Вытягивает истории из коллег",
        "persona": (
            "Пиши в амплуа «Интервьюер (Шаман)». Ты умеешь разговорить людей и вытащить "
            "из них настоящую историю или неожиданную мысль. Строй пост вокруг чужого "
            "голоса: приводи реплику, вопрос и ответ, инсайт собеседника, который цепляет. "
            "Используй приём «я спросил — мне ответили», добавляй живую прямую речь и "
            "свой короткий вывод из услышанного. Пусть читатель почувствует, будто "
            "подслушал искренний глубокий разговор."
        ),
    },
    "provocateur": {
        "title": "Провокатор (Адвокат дьявола)",
        "tagline": "Острые темы и холивары",
        "persona": (
            "Пиши в амплуа «Провокатор (Адвокат дьявола)». Ты берёшь спорный тезис и "
            "бросаешь вызов общепринятому мнению, чтобы разжечь обсуждение. Начинай с "
            "смелого, почти неудобного утверждения, аргументированно защищай непопулярную "
            "позицию, показывай вторую сторону там, где все видят только одну. Провоцируй "
            "мысль, а не оскорбляй; будь острым, но корректным и честным. Заканчивай "
            "открытым вопросом, который тянет читателя спорить в комментариях."
        ),
    },
    "trendhunter": {
        "title": "Тренд-хантер (Вирусолог)",
        "tagline": "Тренды, мемы, виральные форматы",
        "persona": (
            "Пиши в амплуа «Тренд-хантер (Вирусолог)». Ты ловишь свежие тренды, мемы и "
            "виральные форматы и первым связываешь их с темой. Пиши бодро, динамично, "
            "современным языком (уместный интернет-сленг — ок, но без перебора), играй "
            "с форматом: списки, хайповые формулировки, отсылки к тому, что «сейчас у "
            "всех в ленте». Задача — сделать пост лёгким, репостируемым и ощущением "
            "«успей, пока актуально». Держи мысль полезной, а не пустым хайпом."
        ),
    },
    "customer_focused": {
        "title": "Клиентоориентированный",
        "tagline": "Кейсы пользы и счастливые клиенты",
        "persona": (
            "Пиши в амплуа «Клиентоориентированный». Ты показываешь пользу через истории "
            "клиентов: была проблема — нашлось решение — стало лучше. Ставь в центр "
            "человека и его результат, а не себя; описывай реальную боль, путь к решению "
            "и измеримый или эмоциональный итог («стало спокойнее», «сэкономили день»). "
            "Тон заботливый и конкретный, без навязчивой продажи — забота считывается "
            "сама. Заканчивай мыслью о том, как читатель может получить такую же пользу."
        ),
    },
}


class GrokError(Exception):
    """Ошибка обращения к Grok API."""


def _build_prompt(
    topic_key: str,
    extra_instruction: str | None = None,
    role_key: str | None = None,
) -> tuple[str, str]:
    topic = TOPICS[topic_key]
    system_parts = [topic["persona"]]
    if role_key and role_key in ROLES:
        system_parts.append("Амплуа автора для этого поста:\n" + ROLES[role_key]["persona"])
    system_parts.append(
        "Твоя задача — написать ОДИН готовый к публикации пост для соцсети Threads.\n"
        "Требования к посту:\n"
        "- на русском языке;\n"
        "- не длиннее 480 символов (лимит Threads — 500);\n"
        "- цепляющее первое предложение (хук);\n"
        "- конкретная мысль или полезный вывод, а не вода;\n"
        "- живой, экспертный, но человечный тон;\n"
        "- без хэштегов (в Threads они не используются);\n"
        "- без markdown-разметки и без кавычек вокруг всего поста.\n"
        "Верни ТОЛЬКО текст поста, без пояснений."
    )
    system_prompt = "\n\n".join(system_parts)

    user_prompt = f"Сгенерируй пост на тему «{topic['title']}»."
    if role_key and role_key in ROLES:
        user_prompt += f" В роли «{ROLES[role_key]['title']}»."
    if extra_instruction:
        user_prompt += f" Дополнительное пожелание: {extra_instruction.strip()}"
    return system_prompt, user_prompt


def _resolve_api_key(api_key: str | None) -> str:
    api_key = api_key or os.getenv("XAI_API_KEY")
    if not api_key:
        raise GrokError(
            "Не задан XAI_API_KEY. Добавьте ключ в файл .env "
            "(см. .env.example). Ключ можно получить на https://console.x.ai"
        )
    return api_key


def _call_grok(
    messages: list[dict[str, str]],
    api_key: str,
    model: str,
    timeout: int,
) -> str:
    payload = {
        "model": model,
        "messages": messages,
        "temperature": 0.9,
        "max_tokens": 800,
    }
    headers = {
        "Authorization": f"Bearer {api_key}",
        "Content-Type": "application/json",
    }

    try:
        resp = requests.post(XAI_API_URL, json=payload, headers=headers, timeout=timeout)
    except requests.RequestException as exc:
        raise GrokError(f"Ошибка сети при обращении к Grok API: {exc}") from exc

    if resp.status_code != 200:
        raise GrokError(
            f"Grok API вернул ошибку {resp.status_code}: {resp.text[:500]}"
        )

    try:
        data = resp.json()
        content = data["choices"][0]["message"]["content"].strip()
    except (ValueError, KeyError, IndexError) as exc:
        raise GrokError(f"Неожиданный ответ от Grok API: {exc}") from exc

    return content


def generate_post(
    topic_key: str,
    extra_instruction: str | None = None,
    role_key: str | None = None,
    api_key: str | None = None,
    model: str | None = None,
    timeout: int = 60,
) -> str:
    """Сгенерировать текст поста для Threads по выбранной теме (и роли)."""
    if topic_key not in TOPICS:
        raise GrokError(f"Неизвестная тема: {topic_key}")
    if role_key and role_key not in ROLES:
        raise GrokError(f"Неизвестная роль: {role_key}")

    api_key = _resolve_api_key(api_key)
    model = model or DEFAULT_MODEL
    system_prompt, user_prompt = _build_prompt(topic_key, extra_instruction, role_key)

    return _call_grok(
        [
            {"role": "system", "content": system_prompt},
            {"role": "user", "content": user_prompt},
        ],
        api_key=api_key,
        model=model,
        timeout=timeout,
    )


def edit_post(
    base_post: str,
    instruction: str,
    api_key: str | None = None,
    model: str | None = None,
    timeout: int = 60,
) -> str:
    """Переписать существующий пост по указанию пользователя (базой служит base_post)."""
    base_post = (base_post or "").strip()
    instruction = (instruction or "").strip()
    if not base_post:
        raise GrokError("Нет исходного поста для редактирования.")
    if not instruction:
        raise GrokError("Опишите, что изменить в посте.")

    api_key = _resolve_api_key(api_key)
    model = model or DEFAULT_MODEL

    system_prompt = (
        "Ты — редактор постов для соцсети Threads. Тебе дают ГОТОВЫЙ пост "
        "и указание, что в нём изменить. Перепиши пост с учётом указания, "
        "сохраняя исходную тему и смысл, если об обратном не просят.\n"
        "Требования к результату:\n"
        "- на русском языке;\n"
        "- не длиннее 480 символов (лимит Threads — 500);\n"
        "- живой, экспертный, но человечный тон;\n"
        "- без хэштегов (в Threads они не используются);\n"
        "- без markdown-разметки и без кавычек вокруг всего поста.\n"
        "Верни ТОЛЬКО итоговый текст поста, без пояснений."
    )
    user_prompt = (
        f"Вот текущий пост:\n\n{base_post}\n\n"
        f"Внеси изменение: {instruction}"
    )

    return _call_grok(
        [
            {"role": "system", "content": system_prompt},
            {"role": "user", "content": user_prompt},
        ],
        api_key=api_key,
        model=model,
        timeout=timeout,
    )
